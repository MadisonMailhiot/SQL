--

--1. Order the items by price
SELECT *
FROM superstore 
ORDER BY price
;

--2. What is the average price of an item at Superstore?

SELECT AVG(price)
FROM superstore
;

--3. Write your own SQL query that will show a statistic about the price for items in the category of "Kitchen Supplies".

SELECT AVG(price)
FROM superstore
WHERE category = "Kitchen Supplies"
;

--4. Which product has the lowest average rating?
SELECT item_name, average_rating
FROM superstore
ORDER BY average_rating 
LIMIT 1
;
